//! Implements the state container for the scrollbars.

use crate::data::Input;

/// Stores the state of the scrollbars.
pub struct ScrollbarState {
    //hex_start_offset:
}

impl ScrollbarState {
    /// Initializes the scrollbar state.
    pub fn new(input: &Input) -> ScrollbarState {
        ScrollbarState {}
    }
}
